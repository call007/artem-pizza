export enum Path {
  PizzaConfigurator = "/",
  Login = "/log-in",
  Signup = "/sign-up",
  Checkout = "/checkout",
  CheckoutSuccess = "/checkout-success",
  Preview = "/preview",
  Orders = "/orders",
}
