export enum Path {
  PizzaConfigurator = "/",
  PizzaPreview = "/pizza-preview",
  Login = "/log-in",
  Signup = "/sign-up",
  Checkout = "/checkout",
  CheckoutSuccess = "/checkout-success",
  Orders = "/orders",
}
